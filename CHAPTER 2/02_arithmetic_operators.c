#include <stdio.h>

int main(){
int a = 8;
int b = 2;
int c = a/b;
printf("The value of a is %d and value of b is %d and DIVISION is %d", a, b, c);

// This does not work for exponentiation in c
// int d = a^b;not applicable 
return 0;
}

